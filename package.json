{
  "name": "create-catalog",
  "version": "1.0.1",
  "description": "Create a Catalog style guide for your app",
  "main": "index.js",
  "license": "BSD-3-Clause",
  "bin": {
    "create-catalog": "dist/bin/create-catalog.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "dependencies": {
    "args": "^3.0.4",
    "babel-runtime": "^6.26.0",
    "chalk": "^2.1.0",
    "execa": "^0.8.0",
    "mkdirp-then": "^1.2.0",
    "ora": "^1.3.0",
    "sander": "^0.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-core": "^6.24.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.3.2",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.53.1"
  },
  "scripts": {
    "build-watch": "babel src --out-dir dist --watch",
    "build": "babel src --out-dir dist"
  }
}
